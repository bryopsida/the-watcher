import { describe, it, expect } from "@jest/globals"

describe("Notifications", () => {
  describe("notificationServiceFactory", () => {
    describe("build()", () => {
      it("should create the requested notification service", () => {
        expect(false).toBeTruthy()
      })
    })
  })
  describe("slackNotificationService", () => {
    describe("sendReports()", () => {
      it("should notify a channel", () => {
        expect(false).toBeTruthy()
      })
    })
  })
  describe("mattermostNotificationService", () => {
    describe("sendReports()", () => {
      it("should notify a channel", () => {
        expect(false).toBeTruthy()
      })
    })
  })
  describe("teamsNotificationService", () => {
    describe("sendReports()", () => {
      it("should notify a channel", () => {
        expect(false).toBeTruthy()
      })
    })
  })
})
